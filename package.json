{
  "name": "myai-contracts",
  "version": "1.0.0",
  "description": "MyAI on-chain smart contracts — escrow, reputation, governance on Base",
  "scripts": {
    "compile":         "hardhat compile",
    "test":            "hardhat test",
    "deploy:testnet":  "hardhat run scripts/deploy.js --network base-sepolia",
    "deploy:mainnet":  "hardhat run scripts/deploy.js --network base",
    "verify":          "hardhat run scripts/verify.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
